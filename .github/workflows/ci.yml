name: Backend CI Pipeline
on:
  push:
    branches:
      - '**'
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world!
        run: echo "Hello world!"